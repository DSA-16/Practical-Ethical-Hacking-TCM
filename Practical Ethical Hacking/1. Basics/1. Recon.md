1. Subdomain enun:

-> sublist3r -d tesla.com
-> sublist3r -d tesla.com -t 100

2. Subdomain enum (online):
crt.sh(website)
-> %.tesla.com

3. owasp amass(slow, deep info):
-> amass enum -d google.com

4. what web (on kali):
-> whatweb https://google.com

5. Google FU:
searching on google using dorks
